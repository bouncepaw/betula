{{define "title"}}Following{{end}}
{{define "body"}}
    <main>
        {{template "follow tabs" .}}
        <article>
            <h2>Following {{len .Actors}}</h2>
        </article>
        {{range .Actors}}{{template "actor card" .}}{{else}}<article>Empty here...</article>{{end}}
    </main>
{{end}}
