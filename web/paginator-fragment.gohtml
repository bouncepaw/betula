{{define "paginator"}}
	<div class="paginator">
		{{range .Pages}}
			{{if .IsCurrent}}
				<b>{{.Number}}</b>
			{{else}}
				<a href="{{.URL}}"{{if .IsNext}} rel="next"{{else if .IsPrev}} rel="prev"{{end}}>{{.Number}}</a> <!-- TODO: rel=prev rel=next-->
			{{end}}
		{{end}}
	</div>
{{end}}